# Change Log

## 0.2.0

- Extension marketplace metadata, README, and command categories.
- **Register Active Session** reads `.skillpilot/session.json` with correct repo root (parent of `dist/`, not `dist/` itself).
- Session path resolution via `skillpilot.serverEntry` / `skillpilot.skillRoot` and workspace search.

## 0.1.0

- Initial release: status-bar TTL, register from clipboard / wizard, dismiss → MCP cleanup.
- Sprint C / E1 companion for SkillPilot MCP `load` and `begin_task`.
